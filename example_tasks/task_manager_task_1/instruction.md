Jordan Kim (U004) is stepping back from active sprint work. Reassign all tasks currently assigned to her using the following rules:
- If a task belongs to a milestone, reassign it to Morgan Patel (U002).
- If a task has no milestone, reassign it to Riley Stone (U003) and append "needs-triage" to its existing labels (do not replace the other labels).
Return the task IDs you modified as a comma-separated list.
